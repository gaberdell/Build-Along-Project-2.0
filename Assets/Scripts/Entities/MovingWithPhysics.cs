using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MovingWithPhysics : MonoBehaviour
{
    //Ok so Im gonna give you a bit more free range on this one
    //So in order to get this thing moving with physics
    //I have placed a "Character Controller" inside of the object
    //this script is attached to

    //"Character Controller"'s instances be moved using the .Move()
    //function and inputting in a Vector3

    //Hint : the GetComponent function might be helpful here!

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
